% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fish_weight.R
\name{convert_length_to_weight}
\alias{convert_length_to_weight}
\title{Convert fish length to weight using species-specific coefficients}
\usage{
convert_length_to_weight(
  fish_data,
  species_ref,
  length_weight_table = NULL,
  species_conversion = species_code_conversion_4_length_weight(),
  default_coefficients = c(a = 0.01, b = 3.03),
  verbose = FALSE
)
}
\arguments{
\item{fish_data}{A data frame containing fish measurements. Must have columns:
\code{species_code} and \code{size_mm} (length in millimeters).}

\item{species_ref}{A data frame with species reference information. Must have
columns: \code{species_code}, \code{latin_name}.}

\item{length_weight_table}{Optional pre-fetched FishBase length-weight table.
If NULL, fetches from FishBase automatically.}

\item{species_conversion}{Named vector mapping species codes to conversion
species. Names are original codes, values are codes with known coefficients.
Default uses \code{species_code_conversion_4_length_weight()}.}

\item{default_coefficients}{Named vector of default length-weight coefficients
for species without FishBase or conversion data. Default is \code{c(a = 0.01, b = 3.03)}.}

\item{verbose}{Logical. If TRUE, prints progress and warning messages.}
}
\value{
A list containing:
\itemize{
\item \code{fish_data}: Input data frame with added \code{weight_g} column
\item \code{coefficients}: Data frame of coefficients used for each species
\item \code{missing_species}: Species codes that used default coefficients
\item \code{source_summary}: Count of species by coefficient source
}
}
\description{
Converts fish length measurements to weight using length-weight relationship
coefficients from FishBase, species conversion mappings, and default fallback
coefficients. Returns both the converted data and the coefficient table.
}
\details{
The length-weight relationship used is: \eqn{Weight(g) = a \times (Length(cm))^b}

\strong{Priority order for coefficients:}
\enumerate{
\item FishBase direct match (averaged Type I linear regressions for TL)
\item Species conversion (using closely related species)
\item Default coefficients (0.01, 3.03 approximating W = 0.01 * L^3.03)
}

\strong{Note on units:}
\itemize{
\item Input length must be in \strong{millimeters} (converted to cm internally)
\item Output weight is in \strong{grams}
\item FishBase coefficients are applied as-is (expect cm/g relationship)
}
}
\examples{
\dontrun{
# Prepare data
fish_individuals <- data.frame(
  species_code = c("PER", "TRF", "UNKNOWN"),
  size_mm = c(120, 250, 150)
)

species_ref <- data.frame(
  species_code = c("PER", "TRF"),
  latin_name = c("Perca fluviatilis", "Salmo trutta")
)

# Convert
result <- convert_length_to_weight(
  fish_data = fish_individuals,
  species_ref = species_ref
)

# View results
head(result$fish_data)
print(result$coefficients)
print(result$source_summary)
}

}
