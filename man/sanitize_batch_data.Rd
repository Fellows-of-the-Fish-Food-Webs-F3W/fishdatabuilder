% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fish_batches.R
\name{sanitize_batch_data}
\alias{sanitize_batch_data}
\title{Sanitize and validate fish batch data from ASPE database}
\usage{
sanitize_batch_data(
  fish_batch,
  ind_measure,
  species_ref,
  min_individuals_G = 5,
  min_individuals_SL = 10,
  verbose = TRUE
)
}
\arguments{
\item{fish_batch}{A data frame containing fish batch data (from \code{clean_fish_batch()}).
Expected columns: \code{batch_id}, \code{batch_type}, \code{number}, \code{species_code},
\code{min_length}, \code{max_length}}

\item{ind_measure}{A data frame containing individual fish measurements
(from \code{clean_individual_measurement_aspe()}).
Expected columns: \code{batch_id}, \code{size}}

\item{min_individuals_G}{Integer. Minimum number of individuals required for
type "G" batches to generate reliable distributions. Default is 5.}

\item{min_individuals_SL}{Integer. Minimum number of measured individuals required
for type "S/L" batches. Default is 10.}

\item{verbose}{Logical. If TRUE, prints messages about filtered records.}
}
\value{
A list containing:
\itemize{
\item \code{fish_batch}: Cleaned and validated batch data
\item \code{ind_measure}: Filtered measurement data matching cleaned batches
\item \code{filtering_log}: Data frame documenting filtered records
\item \code{validation_issues}: Data frame with specific validation problems
}
}
\description{
Cleans fish batch data from ASPE standard format by filtering invalid records,
checking required columns, and validating batch types and measurements.
Ensures data quality before individual fish size generation.
}
\note{
The function performs the following validations:
\itemize{
\item Removes measurements without matching batch IDs
\item Filters invalid batch types (only G, S/L, I, N are valid)
\item Removes batches with invalid or missing fish counts (number <= 0 or NA)
\item For type G: validates min/max are not NA, min_length <= max_length,
and sufficient sample size (>= min_individuals_G)
\item For type S/L: ensures at least min_individuals_SL measured individuals
\item For types I and N: verifies measured count matches batch count
}
}
\examples{
\dontrun{
# After running cleaning functions
fish_batch_clean <- clean_fish_batch()
ind_measure_clean <- clean_individual_measurement_aspe()

# Sanitize batch data
sanitized <- sanitize_batch_data(fish_batch_clean, ind_measure_clean)

# Inspect any issues
print(sanitized$validation_issues)

# Use with custom thresholds
sanitized_strict <- sanitize_batch_data(
  fish_batch_clean, 
  ind_measure_clean,
  min_individuals_G = 10,
  min_individuals_SL = 20
)
}

}
