% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering_species.R
\name{species_to_remove}
\alias{species_to_remove}
\title{List of species codes to remove from ASPE sampling data}
\usage{
species_to_remove(
  not_fish = TRUE,
  not_determined = TRUE,
  additional_codes = NULL
)
}
\arguments{
\item{not_fish}{Logical. If \code{TRUE} (default), includes codes for non-fish species
such as crayfish and crabs that are captured during fish sampling but should
typically be excluded from fish community analyses.}

\item{not_determined}{Logical. If \code{TRUE} (default), includes codes for
undetermined individuals that lack information for a species-level identification.}

\item{additional_codes}{Character vector. Additional species codes to be added the output}
}
\value{
A character vector of three-letter uppercase species codes to be
excluded from analyses. Returns an empty vector if both parameters are \code{FALSE}.

A character vector of species codes to remove
}
\description{
Generates a vector of species codes (three-letter uppercase codes) that can be
filtered out from datasets, including non-fish species (primarily crayfish)
and undetermined individuals that cannot be reliably assigned to a species.
}
\details{
The function categorizes species codes into two groups:

\strong{Non-fish species} (included when \code{not_fish = TRUE}):
\describe{
\item{ASL}{Crayfish (unspecified)}
\item{CRC}{Chinese crab}
\item{ECR}{Undetermined crayfish}
\item{OCL}{American crayfish}
\item{PCC}{Louisiana crayfish}
\item{OCI}{Crayfish "\emph{Orconectes immunis}"}
\item{PCV}{Crayfish "\emph{Procambarus virginalis}"}
\item{PFL}{Signal crayfish}
}

\strong{Undetermined species} (included when \code{not_determined = TRUE}):
\describe{
\item{COR}{Undetermined \emph{Coregonus} (lake whitefish genus)}
\item{SAL}{Undetermined Salmonidae (salmonid family)}
}
}
\note{
\itemize{
\item These codes correspond to the ASPE database species coding system
\item The function is designed to be used with \code{\link[dplyr:filter]{dplyr::filter()}} or similar
filtering operations to exclude unwanted species from analyses
\item Future versions may include anadromous/catadromous species options
}
}
\examples{
\dontrun{
# Get all species to remove (default)
remove_codes <- species_to_remove()

# Remove only non-fish species, keep undetermined
remove_codes <- species_to_remove(not_determined = FALSE)

# Use in filtering operations
clean_fish_data <- fish_data \%>\%
  dplyr::filter(!species_code \%in\% species_to_remove())
  
# Keep all species (empty removal list)
keep_all <- species_to_remove(not_fish = FALSE, not_determined = FALSE)
}

}
\seealso{
\itemize{
\item \code{\link[=clean_fish_batch]{clean_fish_batch()}} for fish batch data cleaning
\item \code{\link[=clean_individual_measurement_aspe]{clean_individual_measurement_aspe()}} for individual fish measurements
}
}
