% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_community_metrics.R
\name{compute_community_metrics}
\alias{compute_community_metrics}
\title{Compute fish community metrics}
\usage{
compute_community_metrics(fish_data, operation)
}
\arguments{
\item{fish_data}{A data frame containing individual fish records. Must contain
the columns \code{operation_id}, \code{species_code}, and \code{weight_g}. Each row is
expected to represent one individual.}

\item{operation}{A data frame containing operation-level information. Must
contain the columns \code{operation_id} and \code{computed_surface}, with
\code{computed_surface} expressed in square metres.}
}
\value{
A tibble with one row per sampling operation and nine columns:
\itemize{
\item \code{operation_id}: sampling operation identifier.
\item \code{total_richness}: number of unique species.
\item \code{total_abundance}: total number of individuals.
\item \code{total_biomass_g}: total biomass in grams. Missing individual
weights are excluded from the sum.
\item \code{richness_per_m2}: species richness divided by sampled surface area.
Returns \code{NA} when sampled surface is missing, non-finite, or <= 0.
\item \code{abundance_per_m2}: abundance divided by sampled surface area.
Returns \code{NA} when sampled surface is missing, non-finite, or <= 0.
\item \code{biomass_g_per_m2}: biomass divided by sampled surface area.
Returns \code{NA} when sampled surface is missing, non-finite, or <= 0.
\item \code{abundance_by_species}: nested tibble containing \code{species_code},
\code{total_abundance}, and \code{abundance_per_m2}.
\item \code{biomass_by_species}: nested tibble containing \code{species_code},
\code{total_biomass_g}, and \code{biomass_g_per_m2}.
}
}
\description{
Computes community-level richness, abundance, and biomass for each sampling
operation, together with corresponding metrics standardized by sampled
surface area. Species-level abundance and biomass are returned as nested
tibbles.
}
\details{
Only operations represented in \code{fish_data} are returned.

Missing values in \code{weight_g} are excluded from biomass calculations and
generate a warning. Zero values in \code{weight_g} are retained in biomass
calculations and also generate a warning.

Total richness, abundance, and biomass are calculated independently of
sampled surface area. When \code{computed_surface} is missing, non-finite, or
less than or equal to zero, surface-standardized metrics are returned as
\code{NA}.
}
\examples{
fish_data <- tibble::tibble(
  operation_id = c(1, 1, 1, 2),
  species_code = c("AAA", "AAA", "BBB", "AAA"),
  weight_g = c(10, 15, NA, 0)
)

operation <- tibble::tibble(
  operation_id = c(1, 2),
  computed_surface = c(100, 0)
)

compute_community_metrics(
  fish_data = fish_data,
  operation = operation
)

}
