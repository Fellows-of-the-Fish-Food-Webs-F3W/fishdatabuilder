% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_operation.R
\name{filter_operation_batch_measure}
\alias{filter_operation_batch_measure}
\title{Filter sampling operations and associated measurements for spatio-temporal analysis}
\usage{
filter_operation_batch_measure(
  operation = clean_operation_aspe(),
  op_protocol_to_keep = c("complete", "partial_by_point", "partial_over_bank"),
  op_objective_to_exclude = vec_op_objective_to_exclude(),
  oldest_sampling_date = "1995-01-01",
  omit_na_site = TRUE,
  point_group = cleaning_point_group(),
  min_prop_point_group_on_bank = 0.999,
  ele_sampling = cleaning_elementary_sampling(),
  max_passage_number = 1,
  fish_batch = clean_fish_batch(),
  ind_measure = clean_individual_measurement_aspe()
)
}
\arguments{
\item{operation}{A data frame containing cleaned operation data.
By default uses \code{clean_operation_aspe()}.}

\item{op_protocol_to_keep}{Character vector of protocols to retain.
Default: \code{c("complete", "partial_by_point", "partial_over_bank")}.}

\item{op_objective_to_exclude}{Character vector of objective labels to exclude.
By default uses \code{vec_op_objective_to_exclude()}.}

\item{oldest_sampling_date}{Date threshold (character or Date object).
Operations before this date are excluded. Default: "1995-01-01".}

\item{omit_na_site}{Logical indicating whether to exclude operations with
missing site IDs. Default: \code{TRUE}.}

\item{point_group}{A data frame containing cleaned point group data.
By default uses \code{cleaning_point_group()}.}

\item{min_prop_point_group_on_bank}{Minimum proportion of bank-based points
required to consider a "partial_by_point" protocol as "partial_over_bank".
Default: 0.999.}

\item{ele_sampling}{A data frame containing cleaned elementary sampling data.
By default uses \code{cleaning_elementary_sampling()}.}

\item{max_passage_number}{Maximum passage number to include (relevant for
multi-pass sampling protocols). Default: 1 (first passage only).}

\item{fish_batch}{A data frame containing cleaned fish batch data.
By default uses \code{clean_fish_batch()}.}

\item{ind_measure}{A data frame containing cleaned individual fish measurements.
By default uses \code{clean_individual_measurement_aspe()}.}
}
\value{
A list containing three filtered data frames:
\itemize{
\item \code{operation}: Filtered operation data
\item \code{fish_batch}: Filtered fish batch data
\item \code{ind_measure}: Filtered individual measurement data
}
All returned data frames contain only operations and sampling events that
pass all specified filtering criteria.
}
\description{
A comprehensive filtering function that applies multiple criteria to select
operations and their associated measurements suitable for spatio-temporal
analysis. This function implements a multi-step filtering process to ensure
dataset consistency across protocols, objectives, dates, and sampling methods.
}
\details{
The function implements a multi-step filtering process:

\strong{Phase A: Protocol normalization}
Identifies "partial_by_point" operations where all points were sampled
over the bank, reclassifying them as equivalent to "partial_over_bank"
protocol for analysis purposes.

\strong{Phase B: Passage consistency}
Ensures consistent passage numbers across operations, particularly for
complete and partial_over_bank protocols.

\strong{Phase C: Elementary sampling selection}
Combines sampling events from normalized protocols and consistent passages.

\strong{Phase D: Operation filtering}
Applies date thresholds, objective exclusions, site completeness, and
protocol-based filtering to operation data.

\strong{Phase E: Biological data filtering}
Filters fish batch and individual measurement data based on the selected
operations and sampling events.
}
\note{
Additional filtering considerations mentioned in the code (TODO items):
\itemize{
\item Consider station-level consistency checks (sampling scheme, month, frequency)
}
}
\examples{
\dontrun{
# Using default parameters
filtered_data <- filter_operation_batch_measure()

# Custom filtering for a specific analysis
filtered_custom <- filter_operation_batch_measure(
  op_protocol_to_keep = c("complete", "partial_over_bank"),
  oldest_sampling_date = "2000-01-01",
  max_passage_number = 2,
  min_prop_point_group_on_bank = 0.95
)

# Access filtered components
filtered_ops <- filtered_data$operation
filtered_batches <- filtered_data$fish_batch
filtered_measures <- filtered_data$ind_measure
}

}
\seealso{
\itemize{
\item \code{\link[=clean_operation_aspe]{clean_operation_aspe()}} for operation data preparation
\item \code{\link[=cleaning_point_group]{cleaning_point_group()}} for point group data
\item \code{\link[=cleaning_elementary_sampling]{cleaning_elementary_sampling()}} for sampling event data
\item \code{\link[=clean_fish_batch]{clean_fish_batch()}} for fish batch data
\item \code{\link[=clean_individual_measurement_aspe]{clean_individual_measurement_aspe()}} for individual measurements
\item \code{\link[=vec_op_objective_to_exclude]{vec_op_objective_to_exclude()}} for generating objective exclusion lists
}
}
