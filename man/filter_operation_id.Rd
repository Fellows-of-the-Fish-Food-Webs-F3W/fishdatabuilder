% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_operation.R
\name{filter_operation_id}
\alias{filter_operation_id}
\title{Filter operation IDs based on excluded objectives}
\usage{
filter_operation_id(
  op = clean_operation_aspe(),
  objective_to_exclude = vec_op_objective_to_exclude()
)
}
\arguments{
\item{op}{A data frame containing cleaned operation data. By default uses
\code{clean_operation_aspe()} to retrieve processed operation data. Expected to
contain columns: \code{operation_id} and \code{objective}.}

\item{objective_to_exclude}{A character vector of objective labels to exclude.
By default uses \code{vec_op_objective_to_exclude()} which provides sensible
defaults for excluding restoration and population monitoring objectives.}
}
\value{
A vector of operation IDs (type depends on the input \code{operation_id}
column) containing only operations whose objectives are not in the
exclusion list.
}
\description{
Extracts operation IDs that do not have objectives matching a specified
exclusion list. Useful for filtering out operations with specific objective
types (e.g., restoration or population monitoring) from analysis datasets.
}
\details{
The function performs the following steps:
\itemize{
\item Selects only the \code{operation_id} and \code{objective} columns from the
input operation data
\item Unnests the \code{objective} column (if it contains list-columns or
multiple values per operation)
\item Filters out operations where any objective matches the exclusion list
\item Returns a vector of unique operation IDs that pass the filter
}

Note: Operations are excluded if \emph{any} of their objectives match the
exclusion list. Operations can have multiple objectives, and the function
uses \code{tidyr::unnest()} to handle this appropriately.
}
\examples{
\dontrun{
# Using default exclusions (restoration and population monitoring objectives)
valid_op_ids <- filter_operation_id()

# With custom operation data
clean_ops <- clean_operation_aspe()
valid_ids <- filter_operation_id(op = clean_ops)

# With custom exclusion list
custom_exclusions <- c("Suivi de restauration", "Suivi des populations")
custom_ids <- filter_operation_id(objective_to_exclude = custom_exclusions)

# Use in filtering other datasets
filtered_sampling <- sampling_data \%>\%
  filter(operation_id \%in\% filter_operation_id())
}

}
\seealso{
\itemize{
\item \code{\link[=clean_operation_aspe]{clean_operation_aspe()}} for cleaned operation data
\item \code{\link[=vec_op_objective_to_exclude]{vec_op_objective_to_exclude()}} for generating exclusion lists
\item \code{\link[dplyr:filter]{dplyr::filter()}} for general data filtering
\item \code{\link[tidyr:unnest]{tidyr::unnest()}} for handling nested data structures
}
}
