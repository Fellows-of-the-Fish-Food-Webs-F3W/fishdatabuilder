% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_operation.R
\name{clean_description_operation_aspe}
\alias{clean_description_operation_aspe}
\title{Clean and standardize operation description data from ASPE database}
\usage{
clean_description_operation_aspe(
  op_description = get_description_operation_aspe(),
  ref_isolation = get_ref_isolation_operation_aspe(),
  ref_prospection = get_ref_prospection_method_operation_aspe()
)
}
\arguments{
\item{op_description}{A data frame containing operation description data.
By default uses \code{get_description_operation_aspe()} to retrieve raw data.
Expected to contain columns: \code{odp_iso_id_amont}, \code{odp_iso_id_aval}, \code{odp_mop_id}.}

\item{ref_isolation}{A data frame containing isolation type reference data.
By default uses \code{get_ref_isolation_operation_aspe()}.
Expected to contain columns: \code{iso_id}, \code{iso_libelle}.}

\item{ref_prospection}{A data frame containing prospection method reference data.
By default uses \code{get_ref_prospection_method_operation_aspe()}.
Expected to contain columns: \code{mop_id}, \code{mop_libelle}.}
}
\value{
A data frame with cleaned operation description data containing:
\itemize{
\item Standardized columns (without \code{odp_} prefix)
\item Translated isolation types (upstream and downstream)
\item Translated prospection methods
\item Selected and renamed columns based on \code{replacement_operation_description_col()}
}
}
\description{
Processes fishing operation description data by translating isolation types and
prospection methods, joining reference data, and standardizing column names.
}
\details{
The function performs the following transformations:
\itemize{
\item Translates French isolation labels to English using \code{replacement_isolation_label()}
\item Translates French prospection method labels to English using \code{replacement_prospection_label()}
\item Joins isolation and prospection reference data to operation descriptions
\item Removes \code{odp_} prefix from column names
\item Selects and renames columns based on the mapping from \code{replacement_operation_description_col()}
}
}
\examples{
\dontrun{
# Using default data sources
clean_descriptions <- clean_description_operation_aspe()

# With custom data frames
custom_descriptions <- clean_description_operation_aspe(
  op_description = my_descriptions,
  ref_isolation = my_isolation_ref,
  ref_prospection = my_prospection_ref
)
}

}
\seealso{
\itemize{
\item \code{\link[=get_description_operation_aspe]{get_description_operation_aspe()}} for raw data access
\item \code{\link[=get_ref_isolation_operation_aspe]{get_ref_isolation_operation_aspe()}} for isolation reference data
\item \code{\link[=get_ref_prospection_method_operation_aspe]{get_ref_prospection_method_operation_aspe()}} for prospection reference data
\item \code{\link[=replacement_isolation_label]{replacement_isolation_label()}} for isolation type translations
\item \code{\link[=replacement_prospection_label]{replacement_prospection_label()}} for prospection method translations
}
}
