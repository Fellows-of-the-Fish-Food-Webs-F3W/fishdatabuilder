% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_operation.R
\name{cleaning_elementary_sampling}
\alias{cleaning_elementary_sampling}
\title{Clean and standardize elementary sampling data from ASPE database}
\usage{
cleaning_elementary_sampling(
  sampling = get_elementary_sampling_aspe(),
  ref_sampling = get_ref_elementary_sampling_aspe(),
  ref_passage = get_ref_passage_aspe()
)
}
\arguments{
\item{sampling}{A data frame containing elementary sampling data. By default uses
\code{get_elementary_sampling_aspe()} to retrieve raw data. Expected to contain column:
\code{pre_tpe_id} (sampling type reference ID).}

\item{ref_sampling}{A data frame containing sampling type reference data.
By default uses \code{get_ref_elementary_sampling_aspe()}. Expected to contain columns:
\code{tpe_id} (sampling type ID), \code{tpe_libelle} (sampling type label in French).}

\item{ref_passage}{A data frame containing passage reference data.
By default uses \code{get_ref_passage_aspe()}. Expected to contain columns:
\code{pas_id} (passage ID), \code{pas_numero} (passage number).}
}
\value{
A data frame with cleaned elementary sampling data containing:
\itemize{
\item Standardized column names (without \code{pre_} prefix)
\item Translated sampling type labels in English
\item Added passage number information
\item Selected and renamed columns based on \code{replacement_sampling_col()}
}
}
\description{
Processes elementary sampling data by translating sampling method labels,
joining with reference information, standardizing column names, and adding
passage number information.
}
\details{
The function performs the following transformations:
\itemize{
\item Translates French sampling method labels to English using
\code{replacement_detail_sampling_label()} and \code{get_rev_vec_name_val()}
\item Joins translated sampling type reference data with sampling data
\item Removes \code{pre_} prefix from column names for standardization
\item Selects and renames columns based on the mapping from
\code{replacement_sampling_col()}
\item Joins passage number information from reference data
\item Maintains all relevant sampling information with enhanced metadata
}
}
\examples{
\dontrun{
# Using default data sources
clean_sampling <- cleaning_elementary_sampling()

# With custom data frames
clean_sampling <- cleaning_elementary_sampling(
  sampling = my_sampling_data,
  ref_sampling = my_sampling_ref,
  ref_passage = my_passage_ref
)
}

}
\seealso{
\itemize{
\item \code{\link[=get_elementary_sampling_aspe]{get_elementary_sampling_aspe()}} for raw sampling data access
\item \code{\link[=get_ref_elementary_sampling_aspe]{get_ref_elementary_sampling_aspe()}} for sampling type reference data
\item \code{\link[=get_ref_passage_aspe]{get_ref_passage_aspe()}} for passage reference data
}
}
