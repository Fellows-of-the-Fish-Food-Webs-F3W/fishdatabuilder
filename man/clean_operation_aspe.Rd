% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_operation.R
\name{clean_operation_aspe}
\alias{clean_operation_aspe}
\title{Clean and standardize operation data from ASPE database}
\usage{
clean_operation_aspe(
  op = get_raw_operation_aspe(),
  op_objective = get_objective_operation_aspe(),
  ref_objective = get_ref_objective_operation_aspe(),
  ref_protocol = get_ref_protocol_operation_aspe(),
  sampling_point = get_sampling_point_aspe()
)
}
\arguments{
\item{op}{A data frame containing operation data. By default uses
\code{get_raw_operation_aspe()} to retrieve raw data. Expected to contain columns:
\code{ope_id}, \code{ope_pro_id}, \code{ope_pop_id}, and date-related columns.}

\item{op_objective}{A data frame containing operation-objective relationships.
By default uses \code{get_objective_operation_aspe()}. Expected to contain columns:
\code{opo_ope_id}, \code{opo_obj_id}.}

\item{ref_objective}{A data frame containing objective reference data.
By default uses \code{get_ref_objective_operation_aspe()}. Expected to contain columns:
\code{obj_id}, \code{obj_libelle}.}

\item{ref_protocol}{A data frame containing protocol reference data.
By default uses \code{get_ref_protocol_operation_aspe()}. Expected to contain columns:
\code{pro_id}, \code{pro_libelle}.}

\item{sampling_point}{A data frame containing sampling point data.
By default uses \code{get_sampling_point_aspe()}. Expected to contain columns:
\code{pop_id}, \code{pop_sta_id}.}
}
\value{
A data frame with cleaned operation data containing:
\itemize{
\item Standardized columns (without \code{ope_} prefix)
\item Translated protocol labels to English
\item Joined objective information
\item Joined station IDs from sampling points
\item Logical \code{without_fish} column
\item Derived date fields (\code{date_time} as POSIXct, \code{date} as Date)
}
}
\description{
Processes fishing operation data by joining objective and protocol information,
translating labels, standardizing column names, and enhancing with derived date fields.
}
\details{
The function performs the following transformations:
\itemize{
\item Joins operation objectives from reference tables
\item Translates French protocol labels to English using \code{replacement_operation_protocol_label()} (internal function)
\item Joins sampling point station information
\item Removes \code{ope_} prefix from column names
\item Converts \code{without_fish} from character ("t"/"f") to logical (TRUE/FALSE)
\item Creates proper date/time fields using lubridate
\item Selects and renames columns based on \code{replacement_operation_col()} (internal function)
}
}
\examples{
\dontrun{
# Using default data sources
clean_operations <- clean_operation_aspe()

# With custom data frames
clean_ops <- clean_operation_aspe(
  op = my_operations,
  op_objective = my_objectives,
  ref_objective = my_obj_ref,
  ref_protocol = my_protocol_ref,
  sampling_point = my_sampling_points
)
}

}
\seealso{
\itemize{
\item \code{\link[=get_raw_operation_aspe]{get_raw_operation_aspe()}} for raw operation data
\item \code{\link[=get_objective_operation_aspe]{get_objective_operation_aspe()}} for operation-objective relationships
\item \code{\link[=get_ref_objective_operation_aspe]{get_ref_objective_operation_aspe()}} for objective reference data
\item \code{\link[=get_ref_protocol_operation_aspe]{get_ref_protocol_operation_aspe()}} for protocol reference data
\item \code{\link[=get_sampling_point_aspe]{get_sampling_point_aspe()}} for sampling point data
}
}
